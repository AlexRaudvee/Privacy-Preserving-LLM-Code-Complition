# Privacy-Preserving-LLM-Code-Complition
Research and experimentation around privacy preservness for LLM code complitions when comunicating between IDE and Server


## Quickstart

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
jupyter lab
```

Open: `notebooks/privacy_utility_tradeoff.ipynb`